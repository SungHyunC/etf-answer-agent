langgraph>=0.2.0
langchain-core>=0.3.0
scikit-learn>=1.3.0
numpy>=1.24.0
openai>=1.0.0        # OpenAI-compatible 엔드포인트(OpenAI / vLLM / Ollama) 공용 클라이언트

# 웹 데모(app.py)는 표준 라이브러리 http.server 로 동작하므로 웹 프레임워크 의존성이 없다.
