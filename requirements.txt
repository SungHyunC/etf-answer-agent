langgraph>=0.2.0
langchain-core>=0.3.0
scikit-learn>=1.3.0
numpy>=1.24.0
openai>=1.0.0        # OpenAI-compatible 엔드포인트(OpenAI / vLLM / Ollama) 공용 클라이언트
streamlit>=1.36.0    # 클라우드 배포(Streamlit Community Cloud) 진입점 streamlit_app.py

# 로컬 웹 데모(app.py)는 표준 라이브러리 http.server 로 동작하므로 별도 의존성이 없다.
